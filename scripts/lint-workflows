#!/usr/bin/env bash
# actionlint and zizmor over the workflows, plus the pinning check: every
# third-party `uses:` must name a commit SHA with the version in a comment.
set -euo pipefail
# shellcheck source=scripts/_runner.sh
. "$(dirname "$0")/_runner.sh"
ai_scheme_run workflows lint "$@"
