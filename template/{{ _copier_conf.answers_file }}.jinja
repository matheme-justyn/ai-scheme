# Answers for the ai-scheme skeleton layer -- the single source of the
# repository's shape. Copier rewrites this file on every update; edit it only
# when you mean to change an answer, then run `ai-scheme config validate`.
#
# If this project also uses the mechanism layer, there is a second file,
# `config.toml`, at the repository root. Neither layer reads the other's file
# and same-named keys are independent -- see docs/config-boundary.md and
# ADR 0008.
{{ _copier_answers|to_nice_yaml -}}
